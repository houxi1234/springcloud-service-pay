spring:
  profiles:
    active: dev
  application:
    name: PROVIDER-PAY-SERVICE
eureka:
  instance:
    lease-expiration-duration-in-seconds: 3
    lease-renewal-interval-in-seconds: 1
server:
  port: 10001