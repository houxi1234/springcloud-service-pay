<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ce.st.pay.dao.TTestDao">

    <resultMap type="cn.ce.st.pay.entity.TTest" id="TTestMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="username" column="username" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="TTestMap">
        select
          id, username, password
        from stu_test.t_test
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAll" resultMap="TTestMap">
        select
          id, username, password
        from stu_test.t_test
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAllByTest" resultMap="TTestMap">
        select
          id, username, password
        from stu_test.t_test
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="username != null and username != ''">
                and username = #{username}
            </if>
            <if test="password != null and password != ''">
                and password = #{password}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into stu_test.t_test(username, password)
        values (#{username}, #{password})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update stu_test.t_test
        <set>
            <if test="username != null and username != ''">
                username = #{username},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from stu_test.t_test where id = #{id}
    </delete>

</mapper>